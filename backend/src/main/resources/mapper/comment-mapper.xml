<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="comment">

    <select id="selectCommentById" parameterType="long" resultType="kr.co.iei.comment.model.vo.Comment">
        SELECT
            commentId,
            meetingId,
            writerId,
            parentCommentId,
            content,
            createdAt,
            isDeleted
        FROM comments
        WHERE commentId = #{commentId}
    </select>

    <select id="selectComments" resultType="kr.co.iei.comment.model.vo.CommentResponse">
        SELECT
            c.commentId,
            c.writerId,
            c.parentCommentId,
            u.nickname,
            c.content,
            c.createdAt,
            c.isDeleted,
            u.profileImage
        FROM comments c
        JOIN users u ON c.writerId=u.userId
        WHERE c.meetingId=#{meetingId}
            AND c.isDeleted != 1
        ORDER BY c.createdAt ASC
    </select>

    <update id="softDeleteComment" parameterType="map">
        UPDATE comments
        SET
            isDeleted=#{isDeleted},
            content="모임 주최자에 의해 삭제된 댓글입니다."
        WHERE commentId=#{commentId}
        OR parentCommentId = #{commentId}
    </update>

    <insert id="insertComment" parameterType="kr.co.iei.comment.model.vo.Comment">
        INSERT INTO comments(
            meetingId,
            writerId,
            parentCommentId,
            content
        )
        VALUES(
            #{meetingId},
            #{writerId},
            #{parentCommentId},
            #{content}
        )
    </insert>



</mapper>
